#ifndef LIBGREETING_H
#define LIBGREETING_H

/**
 * @brief Returns a greeting string for the given name.
 * 
 * @param name The name to greet.
 * @return A static string containing the greeting.
 */
const char* get_greeting(const char* name);

#endif
